---
name: Claude Config Sync — Pending
description: User plans to set up git-based sync of ~/.claude across work Mac and home ThinkPad
type: project
originSessionId: 4865e46c-bd4f-4544-9aba-76ff8c792f98
---
User wants to sync their `~/.claude` config across two machines: a Mac at work and a ThinkPad at home. Plan is to use a private git repo to sync the config folder. Deferred to the week of 2026-04-27.

**Why:** Wants a unified Claude setup and memory across both devices without re-configuring each time.

**How to apply:** When the topic comes up, pick up from the git sync approach — walk through `git init ~/.claude`, adding a remote, and cloning on the ThinkPad. Note that home paths will differ between machines.
